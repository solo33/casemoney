# CaseMoney 196 — опубликован 12 сентября 2026

Коммит приложения: `bfa5fcec52c2aa6c2471d56aac6a48f36b9b20f5`.
Все три задания [CI](https://github.com/solo33/casemoney/actions/runs/34676004039) успешны.

## Изменения

- Восстановлена UTF-8 кодировка русских строк главной страницы: подпись дня,
  доходы/расходы и сообщения удаления. Проверены подписи сегодняшней и выбранной даты.
- Расширено поле суммы, выровнены дата и общий расход. Комиссия раскрывается
  чекбоксом; выключение очищает её поля и исключает комиссию из запроса.
- Техническое сохранение снимков валютных курсов больше не меняет updated_at.
  Реальные изменения операций по-прежнему обновляют дату редактирования.

## Данные

Перед переключением создан и проверен дамп:
`/srv/solo32/backups/releases/casemoney-196-20260912/database.dump`.
Сохранены контрольная сумма, предыдущий коммит и образ `solo32-casemoney:pre-release-196`.

Из отдельной проверочной базы восстановлены исходные updated_at для 15 704
операций. Скрипт сравнил остальные поля, теги и историю под блокировкой и
проверил результат до commit. Остальные поля не изменились.
12 новых записей исключены, 2 298 старых записей уже имели исходные метки.
Повторный dry-run: 0 кандидатов, 18 002 исходных метки совпадают с резервной копией.

## Проверка публикации

- Локально: lint, build:verify, 20 браузерных сценариев успешны.
- CI: frontend, backend и PostgreSQL migrations/regression tests успешны.
- Сайт возвращает версию `1.1.0-196`, readiness — ready.
- Проверены login/about, PWA manifest и активный service worker.
- На опубликованном интерфейсе проверены русские подписи, ширина суммы,
  раскрытие комиссии, семейные разделы и мобильное меню с изолированными
  тестовыми ответами API, без создания настоящих финансовых операций.
- Ошибок JavaScript нет; схема остаётся `a0193a000002 (head)`.
