# Выпуск 1.1.0-193 — 8 сентября 2026

Обновление опубликовано на Selectel: https://casemoney.ru.

- Коммит приложения: `7763bcaf5501cc4192941bea847f90a67ccc849e`.
- [CI выпуска](https://github.com/solo33/casemoney/actions/runs/34183194181) завершился успешно: backend, PostgreSQL/миграции и frontend/browser.
- Перед переключением проверено восстановление резервной копии в отдельную БД. Счётчики совпали: 10 пользователей, 56 счетов, 17968 транзакций, 77 валютных остатков. Временная БД проверки удалена.
- Резервная копия сохранена на сервере: `/srv/solo32/backups/releases/casemoney-193-20260908/database.dump`; рядом SHA-256 и предыдущий коммит. Предыдущий образ сохранён как `solo32-casemoney:pre-release-193`.
- Миграция применена до `ff1a2b3c4d5e (head)`; заменён только контейнер CaseMoney.
- `/health/ready` вернул `ready`, `/version.json` — `1.1.0-193`; контейнер имеет статус `healthy`.
- Проверка настоящим Chromium на ширине 375 px: `/login` и `/about` открываются с HTTP 200 и без JavaScript-ошибок.

При упаковке устранено ошибочное правило Git `reports/`: оно исключало новые модули отчётов и во frontend, и в backend. Теперь правило `/reports/` относится только к корневым локальным отчётам. Полнота frontend-импортов проверена по Git, backend дополнительно импортирован из чистого архива и из готового Docker-образа. Первый неполный коммит не был установлен в рабочий контейнер.

Существующим пользователям может потребоваться повторный вход из-за ранее добавленной проверки версии пароля. Обновление PWA сохраняет офлайн-очередь; перед перезапуском нужно сохранить открытую форму.
